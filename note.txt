软件设计五大原则：
    1.单一职责原则（SRP）
    2.开闭原则（OCP）
    3.里氏替换原则(LSP)
    4.接口隔离原则(ISP)
    5.依赖倒置原则(DIP)


提升可调试性：
    1.模块化(函数职责单一)
    2.强隔离(函数错误)
    3.内部状态输出（关键行为日志）
    4.关键指标跟踪（追踪资源：内存、句柄、内部数据结构等，指标：吞吐、流量等）